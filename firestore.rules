rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow public read access to content, but only authenticated users can write.
    match /content/{document} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
